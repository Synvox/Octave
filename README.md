# Octave

## Installing Octave
`npm install`

## Running Octave
`npm start`

## Developing Octave
Make sure you have gulp installed with `npm install gulp -g` and then run `gulp` in the project file.
